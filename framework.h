#pragma once

// framework.h — Windows SDK cible + headers communs
// Généré automatiquement pour WindowsDynamicIsland

#define WIN32_LEAN_AND_MEAN   // Exclure les headers Windows rarement utilisés
#include "targetver.h"
#include <Windows.h>
